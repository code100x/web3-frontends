---
title: Collection Showcase
description: Displays a dynamic collection of NFT cards, highlighting key metadata such as images, names, and details from a specified collection. This component is designed to interact with blockchain data to render NFTs dynamically, supporting pagination for efficient browsing.
component: true
---

<ComponentPreview name="collection-stats-demo" />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx code100x-ui@latest add collection-stats
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="nft-card" />

<Step>Update the import paths to match your project setup.</Step>

</Steps>

</TabsContent>

</Tabs>

## Environment Setup

Before using the `CollectionStats` component, ensure you have the `NEXT_PUBLIC_NFTSCAN_KEY` environment variable set in your project. This key is necessary for fetching NFT data from the NFTScan API.

Create or update your `.env` file in the root of your project with the following line:

```env
NEXT_PUBLIC_NFTSCAN_KEY=your_nftscan_api_key_here
```

Replace `your_nftscan_api_key_here` with your actual NFTScan API key. You can obtain this key by registering and creating an API key on the [NFTScan developers portal](https://developer.nftscan.com/).

## Usage

First, import the `CollectionStats` component:

```tsx
import { Collection } from "@/components/ui/collection-stats"
```

Then, use it in your project by passing the required props, like so:

```tsx
<CollectionStats collectionName="Mad Lads" />
```
